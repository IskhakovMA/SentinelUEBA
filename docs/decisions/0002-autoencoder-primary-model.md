# ADR 0002: PyTorch Autoencoder

The primary Stage 0 model is a small PyTorch autoencoder. It is quick to train on CPU, supports reconstruction-error scoring, and leaves room for a later Isolation Forest baseline.

