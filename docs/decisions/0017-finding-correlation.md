# 0017 Finding Correlation

Finding correlation uses `finding-fingerprint-v1` over safe values only.

Windows with the same fingerprint within 60 minutes attach immutable occurrences to the
same open finding. Resolved and false-positive findings are not silently reopened.
