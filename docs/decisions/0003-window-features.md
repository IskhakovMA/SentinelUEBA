# ADR 0003: 15-minute Windows

Feature engineering uses non-overlapping 15-minute windows. The size is short enough to surface spikes and long enough to combine process, network, system metric, and authentication signals.

