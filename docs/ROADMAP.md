# Roadmap

## Stage 0

Synthetic local demo pipeline, PyTorch autoencoder, FastAPI, React, tests, CI, and privacy-first documentation.

## Recommended Stage 1

- Add real Windows collector interfaces behind explicit opt-in commands.
- Add collector contract tests using fixtures, not personal telemetry.
- Add Isolation Forest as a baseline comparison.
- Add model calibration controls in the UI.
- Add signed local artifact metadata.

